---
import Layout from '../layouts/Slide.astro';

const specificConclusions = [
  {
    id: "01",
    title: "Automatización IA",
    desc: "Procesamiento predictivo que elimina la subjetividad manual.",
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect><line x1="8" y1="21" x2="16" y2="21"></line><line x1="12" y1="17" x2="12" y2="21"></line></svg>`
  },
  {
    id: "02",
    title: "Clasificación de Riesgo",
    desc: "Modelos ML que categorizan niveles: Alto, Medio, Bajo.",
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="20" x2="18" y2="10"></line><line x1="12" y1="20" x2="12" y2="4"></line><line x1="6" y1="20" x2="6" y2="14"></line></svg>`
  },
  {
    id: "03",
    title: "Asistente LLM",
    desc: "Soporte inteligente con Ollama para interpretar resultados.",
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>`
  },
  {
    id: "04",
    title: "Agilidad de Respuesta",
    desc: "Optimización de tiempos para mejorar la experiencia del cliente.",
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>`
  },
  {
    id: "05",
    title: "Rastreo IoT-GPS",
    desc: "Monitoreo en tiempo real de los activos financiados.",
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>`
  },
  {
    id: "06",
    title: "Dashboards KPIs",
    desc: "Gestión visual de indicadores clave del portafolio.",
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect><rect x="7" y="7" width="3" height="9"></rect><rect x="14" y="7" width="3" height="5"></rect></svg>`
  }
];
---

<style>
  @import url('https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;600;700;800&display=swap');

  /* --- LAYOUT --- */
  .slide-container {
    width: 100%;
    height: 100vh;
    background-color: #ffffff;
    background-image: radial-gradient(#e2e8f0 1px, transparent 1px);
    background-size: 40px 40px;
    display: flex;
    font-family: 'Manrope', system-ui, sans-serif;
    overflow: hidden;
  }

  .left-bar {
    width: 80px;
    height: 100%;
    background: #1D4E89;
    flex-shrink: 0;
    z-index: 10;
    box-shadow: 4px 0 20px rgba(0,0,0,0.15);
  }

  .main-content {
    flex-grow: 1;
    padding: 2rem 6rem;
    display: flex;
    flex-direction: column;
    position: relative;
    z-index: 5;
    justify-content: center;
  }

  /* --- HEADER --- */
  .header-area {
    margin-bottom: 2rem;
    position: relative;
  }

  .watermark {
    position: absolute;
    top: -65px; left: -20px;
    font-size: 12rem;
    font-weight: 800;
    color: transparent;
    -webkit-text-stroke: 2px rgba(29, 78, 137, 0.08);
    z-index: -1;
    line-height: 1;
    opacity: 0;
    animation: fadeIn 1.5s ease-out forwards;
  }

  h1 {
    font-size: 3.5rem;
    font-weight: 800;
    color: #0f172a;
    margin: 0;
    opacity: 0;
    animation: slideInLeft 0.8s ease-out 0.2s forwards;
  }

  /* --- HERO BANNER --- */
  .general-objective {
    background: linear-gradient(135deg, #1D4E89 0%, #0f172a 100%);
    padding: 1.8rem 2.5rem;
    border-radius: 16px;
    color: white;
    margin-bottom: 3rem;
    box-shadow: 0 20px 40px -10px rgba(30, 58, 138, 0.4);
    display: flex;
    align-items: center;
    gap: 2rem;
    position: relative;
    overflow: hidden;
    z-index: 2;
    
    opacity: 0;
    transform: translateY(30px);
    animation: smoothEntrance 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.4s forwards;
  }

  .general-objective::after {
    content: '';
    position: absolute; top: 0; left: -100%; width: 50%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    transform: skewX(-25deg);
    animation: shine 6s infinite ease-in-out;
  }

  .check-circle {
    width: 50px; height: 50px;
    background: #10b981;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
    box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.7);
    animation: pulseGreen 2s infinite;
  }

  .obj-text strong { display: block; font-size: 1.2rem; margin-bottom: 5px; text-transform: uppercase; letter-spacing: 1px; }
  .obj-text span { font-size: 1rem; opacity: 0.95; line-height: 1.5; display: block; }

  /* CONECTOR */
  .connector-container {
    display: flex;
    justify-content: center;
    margin-top: -3rem;
    margin-bottom: 1rem;
    opacity: 0;
    animation: fadeIn 0.5s ease 0.8s forwards;
  }
  .line-vertical { width: 2px; height: 35px; background: #cbd5e1; }
  .dot-connector { width: 8px; height: 8px; background: #1D4E89; border-radius: 50%; margin-top: -4px; margin-left: -3px; }

  /* --- GRID DE TARJETAS (NUEVA ANIMACIÓN) --- */
  .grid-conclusions {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .card-item {
    background: linear-gradient(145deg, #ffffff 0%, #f8fafc 100%);
    border: 1px solid #e2e8f0;
    border-radius: 16px;
    padding: 1.5rem;
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    box-shadow: 0 10px 25px -5px rgba(0,0,0,0.07);
    overflow: hidden;
    transition: border-color 0.3s ease, box-shadow 0.3s ease; /* Transiciones suaves */
    
    /* Estado inicial */
    opacity: 0;
    transform: translateY(30px);
    
    /* ANIMACIONES: Entrada suave + NUEVO PULSO PREMIUM */
    animation: 
      smoothEntrance 0.7s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards,
      premiumPulseWave 4s ease-in-out infinite; /* Ciclo de 4 segundos */
  }

  /* Decoración Lateral */
  .card-item::before {
    content: '';
    position: absolute; left: 0; top: 0; bottom: 0; width: 4px;
    background: #1D4E89; border-radius: 4px 0 0 4px;
  }

  /* DESFASE SIGNIFICATIVO PARA EFECTO "OLA" */
  /* La segunda animación (el pulso) empieza mucho después en cada tarjeta */
  .card-item:nth-child(1) { animation-delay: 0.6s, 1.0s; }
  .card-item:nth-child(2) { animation-delay: 0.7s, 1.6s; } /* +0.6s */
  .card-item:nth-child(3) { animation-delay: 0.8s, 2.2s; } /* +0.6s */
  .card-item:nth-child(4) { animation-delay: 0.9s, 2.8s; } /* +0.6s */
  .card-item:nth-child(5) { animation-delay: 1.0s, 3.4s; } /* +0.6s */
  .card-item:nth-child(6) { animation-delay: 1.1s, 4.0s; } /* +0.6s */

  /* Brillo Automático rápido */
  .card-item::after {
    content: '';
    position: absolute; top: 0; left: -150%; width: 60%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(29, 78, 137, 0.05), transparent);
    transform: skewX(-25deg);
    animation: shineAuto 5s infinite ease-in-out; /* Más rápido */
  }
  .card-item:nth-child(odd)::after { animation-delay: 1.5s; }
  .card-item:nth-child(even)::after { animation-delay: 3s; }

  .item-header { display: flex; justify-content: space-between; align-items: flex-start; position: relative; z-index: 2; }

  .icon-wrap {
    width: 40px; height: 40px; background: #fff; color: #1D4E89;
    border-radius: 10px; display: flex; align-items: center; justify-content: center;
    box-shadow: 0 4px 8px rgba(0,0,0,0.05); border: 1px solid #f1f5f9;
    /* El icono también reacciona al pulso */
    animation: iconReactivePulse 4s ease-in-out infinite;
  }
  .card-item:nth-child(2) .icon-wrap { animation-delay: 0.6s; }
  .card-item:nth-child(3) .icon-wrap { animation-delay: 1.2s; }
  /* ...etc para sincronizar con la tarjeta */

  .card-number { font-size: 2.5rem; font-weight: 800; color: rgba(29, 78, 137, 0.06); line-height: 0.8; user-select: none; }
  .item-title { font-size: 1rem; font-weight: 800; color: #0f172a; line-height: 1.2; margin-top: 0.5rem; display: block; }
  .item-desc { font-size: 0.85rem; color: #64748b; line-height: 1.5; position: relative; z-index: 2; }

  /* --- NUEVOS KEYFRAMES DINÁMICOS --- */
  
  /* Entrada suave */
  @keyframes smoothEntrance {
    to { opacity: 1; transform: translateY(0); }
  }

  /* NUEVA ANIMACIÓN: OLA DE PULSO PREMIUM */
  /* Combina elevación, escala sutil, sombra intensa y borde azul */


  /* Icono reacciona sincronizado con la tarjeta */
  @keyframes iconReactivePulse {
    0%, 100% { transform: scale(1); color: #1D4E89; background: #fff; }
    50% { transform: scale(1.1); color: #fff; background: #1D4E89; /* Se vuelve azul sólido */ }
  }

  @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
  @keyframes slideInLeft { from { opacity: 0; transform: translateX(-40px); } to { opacity: 1; transform: translateX(0); } }
  @keyframes shine { 0% { left: -100%; } 20% { left: 200%; } 100% { left: 200%; } }
  @keyframes shineAuto { 0% { left: -150%; } 25% { left: 200%; } 100% { left: 200%; } }
  
  @keyframes pulseGreen {
    0% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.4); }
    70% { box-shadow: 0 0 0 10px rgba(16, 185, 129, 0); }
    100% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0); }
  }

</style>

<Layout title="Conclusiones">
  <div class="slide-container">
    <div class="left-bar"></div>

    <div class="main-content">
      
      <div class="header-area">
        <span class="watermark">05</span>
        <h1>Conclusiones</h1>
      </div>

      <div class="general-objective">
        <div class="check-circle">
          <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
        </div>
        <div class="obj-text">
          <strong>Objetivo General Cumplido</strong>
          <span>Se diseñó e implementó exitosamente un sistema inteligente con IA, analítica predictiva e IoT-GPS, logrando una herramienta integral para la reducción de morosidad.</span>
        </div>
      </div>

      <div class="connector-container">
        <div class="line-vertical">
            <div class="dot-connector"></div>
        </div>
      </div>

      <div class="grid-conclusions">
        {specificConclusions.map((item) => (
          <div class="card-item">
            <div class="item-header">
              <div class="icon-wrap" set:html={item.icon}></div>
              <span class="card-number">{item.id}</span>
            </div>
            
            <span class="item-title">{item.title}</span>
            <p class="item-desc">{item.desc}</p>
          </div>
        ))}
      </div>

    </div>
  </div>
</Layout>